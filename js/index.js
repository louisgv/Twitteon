//This script will check if each slide is on, it also help in sliding the canvas so that they fit.
// Each canvas will have some states. And for each canvas, if they are show in full, this script will load the other.
$(document).ready(function(){
        console.log('DOM Loaded (DOMContentLoaded)');
		
     });
